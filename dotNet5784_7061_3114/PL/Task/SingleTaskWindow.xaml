<Window x:Class="PL.Task.SingleTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Task"
        mc:Ignorable="d"
        DataContext="{Binding Task, RelativeSource={RelativeSource Self}, Mode=TwoWay}"
        Title="SingleTaskWindow" Height="850" Width="600">
    <Grid>
        <StackPanel Margin="0,42,0,0">
            <!-- Created At Date -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Created At Date:" VerticalAlignment="Center" Margin="40,0,5,0"/>
                <DatePicker SelectedDate="{Binding CreatedAtDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            </StackPanel>

            <!-- Start Date -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Start Date:" VerticalAlignment="Center" Margin="40,0,35,0"/>
                <DatePicker SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            </StackPanel>

            <!-- Scheduled Date -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Scheduled Date:" VerticalAlignment="Center" Margin="40,0,6,0"/>
                <DatePicker SelectedDate="{Binding ScheduledDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            </StackPanel>

            <!-- Complete Date -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Complete Date:" VerticalAlignment="Center" Margin="40,0,10,0"/>
                <DatePicker SelectedDate="{Binding CompleteDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            </StackPanel>

            <!-- Deadline Date -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Deadline Date:" VerticalAlignment="Center" Margin="40,0,13,0"/>
                <DatePicker SelectedDate="{Binding DeadLineDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            </StackPanel>

            <!-- Forecast Date -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Forecast Date:" VerticalAlignment="Center" Margin="40,0,15,0"/>
                <DatePicker SelectedDate="{Binding ForcastDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            </StackPanel>

            <!-- Id -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Id:" VerticalAlignment="Center" Margin="40,0,70,0"/>
                <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="79"/>
            </StackPanel>

            <!-- Description -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Description:" VerticalAlignment="Center" Margin="40,0,19,0"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="273"/>
            </StackPanel>

            <!-- Alias -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Alias:" VerticalAlignment="Center" Margin="40,0,55,0"/>
                <TextBox Text="{Binding Alias, UpdateSourceTrigger=PropertyChanged}" Width="275" />
            </StackPanel>

            <!-- Remarks -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Remarks:" VerticalAlignment="Center" Margin="40,0,35,0"/>
                <TextBox Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="276"/>
            </StackPanel>

            <!-- Required Effort Time -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Required Effort Time:" VerticalAlignment="Center" Margin="40,0,5,0" Width="124"/>
                <TextBox Text="{Binding RequiredEffortTime, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="155"/>
            </StackPanel>


            <!-- Deliverables -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Deliverables:" VerticalAlignment="Center" Margin="40,0,20,0"/>
                <TextBox Text="{Binding Deliverables, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="273"/>
            </StackPanel>

            <!-- Status -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Status:" VerticalAlignment="Center" Margin="40,0,5,0"/>
                <ComboBox ItemsSource="{Binding Source={StaticResource StatusCollectiontKey}}" SelectedItem="{Binding Status, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0" Width="65"/>
            </StackPanel>

            <!-- Complexity -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Complexity:" VerticalAlignment="Center" Margin="40,0,55,0"/>
                <ComboBox ItemsSource="{Binding Source={StaticResource ExperienceCollectionKey}}" SelectedItem="{Binding Copmliexity, UpdateSourceTrigger=PropertyChanged}" Margin="-40,0,5,0" Width="130"/>
            </StackPanel>

            <!-- Dependencies -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Dependencies:" VerticalAlignment="Center" Margin="40,0,5,0"/>
                <ListView Width="170"       />
                <!-- Assuming you have a control to display dependencies -->
            </StackPanel>

            <!-- Engineer -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Engineer:" VerticalAlignment="Center" Margin="40,0,5,0"/>
                <!-- Assuming you have a control to display engineer details -->
            </StackPanel>

            <!-- Active -->
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Active:" VerticalAlignment="Center" Margin="40,0,5,0"/>
                <CheckBox IsChecked="{Binding Active, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            </StackPanel>

            <!-- Can To Remove -->
            <StackPanel Orientation="Horizontal" Margin="5" Height="28">
                <Label Content="Can To Remove:" VerticalAlignment="Center" Margin="40,0,5,0" RenderTransformOrigin="0.105,-0.904"/>
                <CheckBox IsChecked="{Binding CanToRemove, UpdateSourceTrigger=PropertyChanged}" RenderTransformOrigin="0.605,1.401" Height="15" Width="17"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
